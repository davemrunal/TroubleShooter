<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="troubleshoot.app">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/troubleshooter"
        android:label="@string/app_name"
        android:theme="@style/Theme.Abtab">
        <activity
            android:name=".Dashboard"
            android:configChanges="orientation|screenLayout|keyboard|screenSize"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".FullComplain"
            android:configChanges="orientation|screenLayout|keyboard|screenSize">
        </activity>

        <activity
            android:name=".LoginActivity"
            android:configChanges="orientation|screenLayout|keyboard|screenSize"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".Signup1"
            android:windowSoftInputMode="stateHidden"
            android:configChanges="orientation|screenLayout|keyboard|screenSize">

        </activity>
        <activity
            android:name=".AboutUs"

            android:configChanges="orientation|screenLayout|keyboard|screenSize"/>
        <activity
            android:name=".Signup2"

            android:configChanges="orientation|screenLayout|keyboard|screenSize"/>
        <activity
            android:name=".EditProfile"
            android:windowSoftInputMode="stateHidden"
            android:configChanges="orientation|screenLayout|keyboard|screenSize"/>

    </application>

</manifest>
